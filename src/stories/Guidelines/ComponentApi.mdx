import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Guidelines/Component API" />

# Component API

Padrões utilizados na construção dos componentes.

---

## Estrutura

```txt
component/
├─ component.ts
├─ component.html
├─ component.scss
└─ component.stories.ts
```

---

## Inputs

Utilizar Angular Input Signals.

```ts
title = input('');
disabled = input(false);
```

---

## Outputs

Utilizar Output API moderna.

```ts
saved = output<void>();
```

---

## Estado Interno

Utilizar Signals.

```ts
isOpen = signal(false);
```

---

## Computed

Utilizar `computed` para estados derivados.

```ts
buttonClasses = computed(() => [
  'ui-button',
  this.disabled() ? 'ui-button--disabled' : '',
].filter(Boolean).join(' '));
```

---

## Templates

Utilizar:

```html
@if (condition()) {
  <span>Content</span>
}
```

```html
@for (item of items(); track item.id) {
  <span>{{ item.title }}</span>
}
```

Evitar:

```html
*ngIf
```

```html
*ngFor
```

---

## Estilos

Utilizar tokens globais:

```scss
color: var(--ui-color-primary);
padding: var(--ui-space-4);
border-radius: var(--ui-radius-md);
```

Nunca utilizar valores hardcoded quando existir token correspondente.

---

## Stories

Cada componente deve possuir um `PlaygroundCompleto` (nome de exibição: **Playground completo**) como story interativa principal, além de:

- Default / estados especiais
- Casos de uso reais
- Variações visuais importantes

Use `includeStories` começando por `PlaygroundCompleto|...` para listar no sidebar apenas as stories públicas (evita exports auxiliares virarem stories).

Objetivo: documentar visualmente todas as possibilidades do componente.
